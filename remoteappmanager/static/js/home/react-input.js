import React from 'react';
import ReactDOM from 'react-dom';

const element = (
    <li className="header">
        APPLICATIONS FROM REACT
    </li>
);

ReactDOM.render(
    element,
    document.getElementById('react-root')
);